import axios from "axios";
import People from "../../models/people.model.js";

export const getVariablePeople = async (req) => {
    try {

       

    } catch (error) {
        throw new Error(error);
    }
}